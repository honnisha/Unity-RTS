## The Blaze Graphics Library

Depends On: Nothing

Blaze is used to perform core graphics operations like rasterisation (GPU and CPU) and triangulation too. It's used by things like InfiniText and, for example, canvas in PowerUI.